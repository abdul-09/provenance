{
  "name": "@provenance/browser",
  "version": "0.1.0",
  "type": "module",
  "description": "Browser capture layer: records a writing session and seals it into a certificate the Python verifier accepts.",
  "scripts": {
    "test": "node --test --experimental-strip-types 'test/*.test.ts'"
  }
}
